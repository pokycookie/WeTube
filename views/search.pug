extends layouts/main
include mixins/videoBlock

block content
    .search_header
        h3 Searching for: #{searchQuery}
    .search__videos
        each item in videos
            +videoBlock({
                title: item.title,
                views: item.views,
                file: item.videoFile,
            }) 